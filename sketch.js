var rickyaSlider, godellSlider, billSlider;

function setup(){
    createCanvas(600,400);
    background(0);
    
    rickyaSlider = createSlider(0, 255, 255);
    rickyaSlider.position(20,20);
    
     godellSlider = createSlider(0, 255, 255);
    godellSlider.position(20,50);
    
     billSlider = createSlider(0, 255, 255);
    billSlider.position(20,80);
}

function draw(){
    
}